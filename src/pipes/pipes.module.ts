import { NgModule } from '@angular/core';
import { RuntimePipe } from './runtime/runtime';

@NgModule({
	declarations: [RuntimePipe],
	imports: [],
	exports: [RuntimePipe]
})
export class PipesModule {}
